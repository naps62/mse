#!/bin/bash

set -e

source ./bin/utils/set-env

ARGS=$@

COMPOSE="docker-compose -f docker-compose.yml"

case "$@" in
  "iex" )
    $COMPOSE exec web /opt/app/bin/mse remote_console
    ;;

  "db-restore")
    ./bin/db-restore/s3-to-prod
    ;;

  * )
    $COMPOSE "$@"
    ;;
esac
