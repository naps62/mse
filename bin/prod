#!/bin/bash

set -e

source ./bin/utils/set-env

ARGS=$@

case "$@" in
  "iex" )
    docker-compose exec web /opt/app/bin/mse remote_console
    ;;

  "db-restore")
    ./bin/utils/db-restore
    ;;

  * )
    docker-compose "$@"
    ;;
esac
