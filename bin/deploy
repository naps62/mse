#!/bin/bash

set -e

source ./bin/utils/set-env

TAG=$MSE_VERSION

if [ "$TAG" == "" ]; then
  echo "No tag given"
  exit
fi

echo $POSTGRES_USER
mix docker.build
mix docker.release
mix docker.publish --tag $TAG

git tag $TAG
git push --tags &

./bin/prod build
./bin/prod stop web
./bin/prod up -d
