// import {Socket} from "phoenix";

// let socket = new Socket("/socket", {params: {token: window.userToken}});

// socket.connect();

// // Now that you are connected, you can join channels with a topic:
// let channel = socket.channel("sets", {})
// channel.join()
//   .receive("ok", resp => { console.log("Joined sets channel successfully", resp) })
//   .receive("error", resp => { console.log("Unable to join sets channel", resp) })

// export default socket;
